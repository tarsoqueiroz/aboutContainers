apiVersion: v1
kind: Secret
metadata:
  name: app-secret
data:
  username: dGFyc28=
  password: Y2hhbmdlSXQxMjM=
---
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  restartPolicy: Never
  containers:
  - name: ubuntu
    image: ubuntu
    resources:
      limits:
        memory: "512Mi"
        cpu: "500m"
      requests:
        memory: "64Mi"
        cpu: "250m"
    env:
    - name: "app_username"
      valueFrom:
        secretKeyRef:
          name: app-secret
          key: username
    - name: "app_password"
      valueFrom:
        secretKeyRef:
          name: app-secret
          key: password
    args:
    - env            