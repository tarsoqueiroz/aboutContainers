apiVersion: v1
kind: Pod
metadata:
  name: nginx3
  labels:
    zone: prod
    version: v1
  annotations:
    description: Apasoft-Training Openshift Course
spec:
  containers:
   - name: nginx   
     image: nginx
     resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"
